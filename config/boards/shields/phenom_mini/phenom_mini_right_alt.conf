# ALT build: faithful badjeff/zmk-pmw3610-driver (v0.3) algorithm on the
# ergohaven bit-bang polling transport.  Passed via -DEXTRA_CONF_FILE so the
# normal phenom_mini_right.conf (USB logging) is overridden.  No FSEG/VECT/
# report-window flags: ALT_MODE replaces the whole pipeline.
CONFIG_EH_PMW3610_ALT_MODE=y
CONFIG_ZMK_USB_LOGGING=n